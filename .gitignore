__pycache__/
*.pyc
# Generated datasets are deterministic (seeded) and regenerated in CI — never committed.
# Covers both linear (data/scm_t/<config>) and the legacy data/linearscm_t/ layout.
data/
# Plan-runner / autonomous-execution runtime logs.
logs/
.env
wandb/
.DS_Store
dist/
*.egg-info/

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# Test artefacts
.pytest_cache/
.coverage
htmlcov/

# Experiment outputs — regenerated by experiments/run_all.py per config; the
# nonlinear (smoke_nl) run clobbers these in place, so they are not tracked.
experiments/results.json
experiments/per_instance.csv
# Per-config snapshots written by experiments/run_suite.py (results.json +
# per_instance.csv + figures/ per config). Regenerated by re-running the suite.
experiments/results_archive/

